parameters:
  kapitan:
    compile:
      - input_paths:
          - system-upgrade-controller/component/app.jsonnet
        input_type: jsonnet
        output_path: apps/
      - input_paths:
          - system-upgrade-controller/component/main.jsonnet
        input_type: jsonnet
        output_path: system-upgrade-controller/
      - input_paths:
          - system-upgrade-controller/component/alertrules.jsonnet
        input_type: jsonnet
        output_path: system-upgrade-controller/
